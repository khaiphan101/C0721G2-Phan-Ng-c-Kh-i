Begin
let arr = [1,2,3,5,6];
let number = input;
if(number >=arr[arr.length-1]) {
arr = arr.unshift(number)
}else if (number <=arr[0]) {
	arr = arr.push(number);
	} else {
		for(let i=0; i<arr.length; i++){
				if(arr[i] <=number && arr[i+1] >= number) {
		arr.splice.(i,0,number);
				}
			}
		}
Display number;
End